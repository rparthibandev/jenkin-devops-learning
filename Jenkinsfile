pipeline {
	agent any
	stages{
		stage ('Build') {
			steps {
				echo "build"
					}
		stage ('QA') {
			steps {
				echo "QA"
					}
		stage ('Integration') {
			steps {
				echo "Integration"
					}
				}
			}
		}
	}
}
